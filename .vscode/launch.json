{
    "version": "0.2.0",
    "configurations": [
         {
            "name": "Docker: Attach to Node",
            "type": "python",
            "request": "attach",
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/code"
        }
    ]
}